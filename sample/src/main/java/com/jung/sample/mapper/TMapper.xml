<?xml version="1.0" encoding="UTF-8"?>
<!-- DOCTYPE엘리먼트 -->
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <!-- mapper.xml문서의 최상위 엘리먼트로 이 안에다 sql처리를 만들어 줌 --> 
 <!-- namespace속성은 interface의 패키지포함 경로 -->  

<mapper namespace="com.jung.sample.dao.TDao">
 	
 	<!-- 회원가입 -->
 	<insert id="join"  parameterType="com.jung.sample.dto.JoinDto">
 		INSERT INTO JOININFO (USERID, USERPW, USERNAME, USEREMAIL, USERPHONE, USERAGREE1, USERAGREE2) 
		VALUES (#{uId}, #{uPw}, #{uName}, #{uEmail}, #{uPhone}, #{uAg1}, #{uAg2})
 	</insert>
 	
 	<!-- 회원가입 sample 아이디 중복체크 -->
 	<select id="IdCheck" resultType="String" parameterType="com.jung.sample.dto.JoinDto">
 		SELECT USERID
 		FROM JOININFO
 		WHERE USERID = #{uId}
 	</select>
 
 
</mapper>